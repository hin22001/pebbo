import WeeklyReportsClient from "../WeeklyReportsClient";

export const metadata = {
  title: "Weekly Reports — Pebbo",
  description: "View your weekly learning reports in Pebbo.",
};

export default function WeeklyReportsTablePage() {
  return <WeeklyReportsClient />;
}
